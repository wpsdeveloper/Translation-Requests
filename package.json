{
  "author": "",
  "devDependencies": {
    "@eslint/css": "^1.2.0",
    "@eslint/js": "^10.0.1",
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.8",
    "clasp": "^1.0.0",
    "dotenv": "^17.4.2",
    "eslint": "^10.3.0",
    "globals": "^17.5.0",
    "jsdom": "^29.1.1",
    "shx": "^0.4.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.10",
    "vite-plugin-gas": "^0.5.2",
    "vite-plugin-singlefile": "^2.3.3",
    "vitest": "^4.1.5"
  },
  "keywords": [],
  "license": "ISC",
  "main": "eslint.config.js",
  "name": "translation-requests",
  "scripts": {
    "build": "npm run clean && npm run build:client && npm run build:server",
    "build:client": "vite build",
    "build:server": "tsc --project tsconfig.server.json && shx cp src/server/*.html dist/ && shx cp appsscript.json dist/",
    "clean": "shx rm -rf dist && shx mkdir dist",
    "deploy": "npm run build && clasp push",
    "props:sync": "node scripts/sync-props.js",
    "dev": "vite --host --port 5173",
    "test": "vitest"
  },
  "version": "1.0.0",
  "description": ""
}
